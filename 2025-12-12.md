### 배열에 참조값 대입
- 내가 만든 클래스의 타입의 변수는 인스턴스의 참조값을 보관한다.
- 배열의 각각의 항목도 참조 타입의 변수이기 떄문에 참조 타입의 참조값을 보관한다.
- 배열에 아직 참조값을 대입하지 않았을 때엔 참조값이 없다는 의미의 null 값으로 초기화 된다.

__자바에서의 대입은 항상 변수에 들어있는 값을 복사한다.__
- 주의할점은 변수에 인스턴스 자체가 들어있는 것이 아니라 인스턴스의 위치를 가리키는 참조값이 들어있을 뿐이다.